// This can be extended later for cart functionality
document.querySelectorAll("button").forEach(btn => {
    btn.addEventListener("click", () => {
      alert("Item added to cart (dummy)");
    });
  });